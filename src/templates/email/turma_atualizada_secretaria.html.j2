<!doctype html>
<html>
  <body style="font-family:Arial,Helvetica,sans-serif;">
    <p>A turma do treinamento <strong>{{ treinamento_nome }}</strong> foi atualizada.</p>
    <ul>
    {% for campo, valores in diff.items() %}
      <li><strong>{{ campo }}:</strong> {{ valores[0] }} → {{ valores[1] }}</li>
    {% endfor %}
    </ul>
  </body>
</html>
