<!doctype html>
<html>
  <body style="font-family:Arial,Helvetica,sans-serif;">
    <p>Uma nova turma do treinamento <strong>{{ treinamento_nome }}</strong> foi cadastrada.</p>
    <ul>
      <li><strong>Data(s):</strong> {{ data_inicio }}{% if data_fim and data_fim != data_inicio %} a {{ data_fim }}{% endif %}</li>
      <li><strong>Horário:</strong> {{ horario }}</li>
      <li><strong>Local:</strong> {{ local_realizacao }}</li>
      <li><strong>Instrutor:</strong> {{ instrutor_nome or 'A definir' }}</li>
    </ul>
  </body>
</html>
