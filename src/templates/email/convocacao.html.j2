<!doctype html>
<html>
  <body style="font-family:Arial,Helvetica,sans-serif;">
    <p>Olá {{ nome_inscrito }},</p>

    <p>Você está convocado(a) para o treinamento <strong>{{ nome_treinamento }}</strong>.</p>

    <ul>
      <li><strong>Data(s):</strong> {{ data_inicio }}{% if data_fim and data_fim != data_inicio %} a {{ data_fim }}{% endif %}</li>
      <li><strong>Horário:</strong> {{ horario }}</li>
      <li><strong>Carga horária:</strong> {{ carga_horaria }} horas</li>
      <li><strong>Instrutor:</strong> {{ instrutor }}</li>
      <li><strong>Local de Realização:</strong> {{ local_realizacao }}</li>
    </ul>

    {% if teoria_online %}
      <p><strong>Teoria:</strong> Online na plataforma
      <a href="{{ plataforma_url }}">{{ plataforma_url }}</a>
      (usuário: {{ usuario_login }} | senha inicial: {{ senha_inicial }}).</p>
      {% if tem_pratica %}
        <p><strong>Prática:</strong> Presencial no Local de Realização: {{ local_realizacao }}.</p>
      {% endif %}
    {% else %}
      {% if tem_pratica %}
        <p><strong>Teoria:</strong> Presencial no Local de Realização: {{ local_realizacao }}.</p>
        <p><strong>Prática:</strong> Presencial no Local de Realização: {{ local_realizacao }}.</p>
      {% else %}
        <p><strong>Treinamento:</strong> Presencial no Local de Realização: {{ local_realizacao }}.</p>
      {% endif %}
    {% endif %}

    <p>Em caso de dúvidas, responda este e-mail.</p>
    <p>Atenciosamente,<br/>SENAI — Sistema FIEMG | Cursos e Treinamentos</p>
  </body>
</html>
